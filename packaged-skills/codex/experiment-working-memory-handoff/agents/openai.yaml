interface:
  display_name: "Experiment Working Memory + Handoff"
  short_description: "Maintain working memory and handoff logs across experiment sessions"
  default_prompt: "Use $experiment-working-memory-handoff to read existing working memory and handoff log at the start of each turn, then append timestamped evidence-backed updates to both after executing any checks or experiments."
